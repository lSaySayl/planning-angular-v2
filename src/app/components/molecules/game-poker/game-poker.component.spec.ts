/* import { ComponentFixture, TestBed } from '@angular/core/testing';

import { GamePokerComponent } from './game-poker.component';

describe('GamePokerComponent', () => {
  let component: GamePokerComponent;
  let fixture: ComponentFixture<GamePokerComponent>;

  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [GamePokerComponent]
    });
    fixture = TestBed.createComponent(GamePokerComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

});
 */
